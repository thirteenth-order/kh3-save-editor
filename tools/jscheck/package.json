{
  "name": "kh3save-jscheck",
  "private": true,
  "description": "Dev-only checks for the browser half of the UI. Nothing here is served, bundled or shipped: the page is still classic scripts with no build step, and the binary embeds the assets as written.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  }
}
